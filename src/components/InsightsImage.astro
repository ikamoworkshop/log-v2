---
/* @/components/Image.astro */
import urlForImage from '../sanity/imageBuilder'

const { asset } = Astro.props;
const image = urlForImage(asset);
---

<img src={image} class="insight-content-image"/>